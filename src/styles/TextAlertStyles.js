import {StyleSheet} from 'react-native';

const TextAlertStyles = StyleSheet.create({
  textoAlerta: {
    fontWeight: 'bold',
    color: 'red',
    marginRight: 10,
  },
});

export default TextAlertStyles;
