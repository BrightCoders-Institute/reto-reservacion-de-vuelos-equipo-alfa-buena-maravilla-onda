import { StyleSheet } from 'react-native';

const DatePickerCalendarStyles = StyleSheet.create({
  modalView: {
    height: '20%',
    padding: 10,
    alignItems: 'center',
    shadowColor: '#000',
  },
});

export default DatePickerCalendarStyles;
